<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.projects.company.homes_lock">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.device.activity.DeviceActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.aboutus.AboutUs.AboutUs" />
        <activity android:name=".ui.login.activity.LoginActivity" />
        <activity android:name=".ui.proservices.activity.ProServicesActivity" />
        <activity android:name=".ui.aboutus.AboutUsActivity" />
        <activity android:name=".ui.splash.SplashActivity" />
        <activity android:name=".ui.support.SupportActivity" />
        <activity android:name=".ui.notification.activity.NotificationActivity" />
        <activity android:name=".ui.setting.activity.SettingActivity"></activity>
    </application>

</manifest>